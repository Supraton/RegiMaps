<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Tema personalitzat -->
    <style name="Theme.MyApplication" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Estil personalitzat per als botons del mapa -->
    <style name="MapButton" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">48dp</item>
        <item name="android:layout_weight">1</item>
        <item name="iconTint">@color/black </item> !-- Color de la icona -->
        <item name="rippleColor">@color/white_20</item> <!-- Efecte ripple -->
    </style>

</resources>